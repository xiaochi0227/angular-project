import { CanshowDirective } from './canshow.directive';

describe('CanshowDirective', () => {
    it('should create an instance', () => {
        // const directive = new CanshowDirective();
        // expect(directive).toBeTruthy();
    });
});
